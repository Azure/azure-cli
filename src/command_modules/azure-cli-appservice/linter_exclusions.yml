---
# exclusions for the vm module

webapp auth update:
    parameters:
        action:
            rule_exclusions:
            - missing_parameter_help_rule
        enabled:
            rule_exclusions:
            - missing_parameter_help_rule
