version: 1
content:
- command:
    name: find
    summary: Find Azure CLI commands.
    examples:
    - summary: Search for commands containing 'vm' or 'secret'
      command: >
        az find -q vm secret
